# Tests

Use this directory for test suites outside package-local Go tests.

- `unit`: domain and application test helpers or cross-package tests
- `integration`: database, container, or adapter-level tests
- `e2e`: full API or workflow tests

